# OrQuanta — Railway Deployment Requirements
# Lean set: only what's needed to run the API in demo mode on Railway

# Core framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
python-dotenv>=1.0.0
emails>=0.6

passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# HTTP
httpx>=0.27.0

# Database (SQLite for demo — no PostgreSQL needed)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Utils
python-dotenv>=1.0.0
tenacity>=8.2.0

# WebSockets
websockets>=12.0
